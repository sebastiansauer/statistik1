Question
========
Was ist das Ergebnis der Logikprüfung `x <- 5; x == 6`?

Answerlist
----------
* TRUE
* FALSE
* 5
* 6
* R gibt eine Fehlermeldung aus, da `x` bereits als 5 definiert wurde.

Solution
========
Der Befehl `x <- 5` weist der Variable den Wert 5 zu. Die Prüfung `x == 6` fragt R: "Ist der Inhalt von x gleich 6?". Da dies nicht der Fall ist, ist das Ergebnis der logische Wert `FALSE`.

Answerlist
----------
* Falsch
* Richtig
* Falsch
* Falsch
* Falsch

Meta-information
================
exname: Logikprüfung_Anwendung
extype: schoice
exsolution: 01000
exshuffle: TRUE

